{
  "name": "confidential-por-midnight",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Confidential Proof of Reserves — Midnight Compact solvency circuit (Rise In Level 1)",
  "engines": { "node": ">=22" },
  "scripts": {
    "build:contract": "compact compile --skip-zk contract/src/por.compact contract/managed",
    "test": "vitest run",
    "pretest": "pnpm build:contract"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.16.0",
    "@midnight-ntwrk/onchain-runtime-v3": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.6"
  }
}
